<form method="post" action="{{ path('app_reto_delete', {'id': reto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reto.id) }}">
    <button class="btn">Delete</button>
</form>
